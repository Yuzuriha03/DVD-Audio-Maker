# 试过但没接入的改动

这些补丁**不在构建流程里**。保留是为了两件事：一是记下「已经排除过这条假设」，
避免重复试验；二是万一将来证据变了，代码还在。

停用理由都与**静图（播放时的专辑封面）**有关 —— 2026-09-22/23 排查「静图不出图
/ 只出第一张 / 上一曲下一曲错乱」时逐个试过。结论是：**照抄商业盘的字段反而会
把静图弄坏**，因为三张商业盘彼此就不一致（见下）。

## 停用清单

| 补丁 | 改了什么 | 停用理由 |
|---|---|---|
| `patch_asvs_header_mode.py` | ASVS `0x0E` → `0x0000` | 实测会坏静图。15:48 那版（静图正常）用的是上游原值 `0x0012` |
| `patch_asvs_palette.py` | ASVS 调色板 → `00101010 × 16` | 实测会坏静图。正常版用的是菜单配色（上游 `--active*-palette`） |
| `patch_still_bitrate.py` | 静图编码加 `-b 3200` 降码率 | 依据是「ASVS 总量 2048 扇区上限」，但那是在硬件加速崩溃期测的，不可靠。用户要高质量图 |
| `patch_still_headers.py` | 静图 `progressive_sequence` → 1、声明码率 → 9000 | 依据成立（商业盘确实都是 1），但**无条件改**和写死一样不可靠。已由 `_merged/patch_mpeg2_autodetect.py` 按实际画面**自检后**填写取代 |
| `patch_still_ntsc.py` | 静图改 720×480 NTSC | 是「PAL 盘有问题」假设的一部分。制式已由 `patch_mpeg2_autodetect.py` 按码流自检，不必写死 |
| `patch_asvs_video_attr.py` | ASVS `0x18` → `0x43`（NTSC） | 同上。它曾把 PAL 盘声明成 NTSC（「声明 NTSC 实播 PAL」），已由自检取代 |
| `disc_spec_versions.py` | 四个 IFO 版本号 0x12 → 0x11 / 0x00 | 见下 |

### ⚠️ 已平反：`patch_stills_per_track_rank.py`

这个补丁曾列在这里（理由写的是「`0x04` 是可翻页幻灯片」），
**2026-09-24 发现理由不成立，已移回 `_merged/`**：

- 它的 `0x04` 只是**注释里解释了为什么不能写 0x04**；
  而且上游本来就只在传了 `--stilloptions` 时才置 0x04 ——
  本工程**从不传**这个选项，实测 byte1 一直是 `0x00`
- 它真正做的事（静图表两个偏移字段改为按轨递进）**是对的**：
  李娜/巴赫两张封面正常的商业盘都是按轨递进的，
  上游的常量写法使同一专辑内所有轨都指回第 1 张图
- 当初把它与其它几个一起停用（回滚到 G 形态），直接导致了
  「同一专辑只有第一首显示封面」这个 bug（用户 2026-09-24 报告）

**教训**：把几个补丁一起停用时，每个都要单独确认理由；
一个补丁里包含两部分改动时，不能因为其中一部分可疑就连另一部分一起丢。
停用后也应当有一个「当初为什么停用」的**实测**依据，而不是推断。

### `disc_spec_versions.py` 为什么单独拿出来

它原名 `02_build.py` 里的 `fix_spec_versions()`，整理时从构建脚本移出。

关键矛盾：**它的 ASVS 规则和 `patch_asvs_header_mode.py` 是同一个改动**
（u16 `0x0E` 从 `0x0012` 改成 `0x0000`）。一个被认定「会坏静图」，另一个却
留在管线里（虽然调用处写着 `if False`）。既然证据指向有害，就一并停用。

有据可查的部分：G 版基础上只改这 14 字节，用户实测**不再崩溃**。
所以「版本号 0x12 与崩溃相关」有一定证据，但「改成 1.1 后静图能否显示」
从没得到正面结论。要重新验证，把文件放回 `scripts/` 并在 `build_disc()`
打包 ISO 前调用即可（幂等）。

## `_experiments_18xx/` —— 更早期的结构试验

2026-09-22 18:04~18:32 的一批「照着商业盘猜结构」的实验，全部未接入：

| 补丁 | 猜的是什么 | 结果 |
|---|---|---|
| `patch_asvs_per_track.py` | 把 ASVS 记录从「按 title」改成「按轨」 | 与商业盘不同构，实测无效 |
| `patch_asvs_per_image.py` | ASVS 每图一条记录 | 同上 |
| `patch_ats_ptt_srpt.py` | 补写 ATS 的 `PTT_SRPT` 表 | **表结构是猜的，会让 PowerDVD 崩溃** |
| `patch_ats_album_rank.py` | 每专辑一个 rank | 未验证 |
| `patch_ats_still_manual.py` | 静图手工指定 | 未验证 |
| `patch_ats_still_pertrack.py` | 静图表按轨 | 未验证 |

## ⚠️ 重要教训：商业盘彼此并不一致

排查时反复踩到的一点 —— **不能把某一张商业盘的某个字段当成「标准」**：

| 字段 | 巴赫 | 李娜 | Enigma |
|---|---|---|---|
| ASVS `0x18` 视频属性 | `0x43` NTSC | `0x43` NTSC | `0x53` PAL |
| ASVS `0x0E` | `0x0000` | `0x0000` | `0x0012` |
| ASVS 调色板 | `00101010 × 16` | `00101010 × 16` | `00108080 × 16` |
| 菜单 `AUDIO_TS.VOB` 的 `progressive_sequence` | （无菜单） | **0** | **1** |
| VIDEO_TS | 有（340 MB 音乐会实况） | 无 | 无 |

前四行里，凡是「两张一致、一张不同」的字段，都是**该盘自己的编码参数**，
不是规范要求。照抄哪一张都可能对不上自己盘的实物。

正确的做法是**从自己的码流里读出这些值**（`patch_mpeg2_autodetect.py` 就是
这么做的），而不是挑一张商业盘抄。
